"use strict";
Object.defineProperty(exports, Symbol.toStringTag, { value: "Module" });
const rehypeParse = require("rehype-parse");
const unified = require("unified");
const rehypeRichText = require("./lib/rehypeRichText.cjs");
const htmlAsRichText = (html, config) => {
  const { result, messages } = unified.unified().use(rehypeParse, { emitParseErrors: true, missingDoctype: 0 }).use(rehypeRichText.rehypeRichText, config).processSync(html);
  return { result, warnings: messages.map((message) => message.toString()) };
};
exports.htmlAsRichText = htmlAsRichText;
//# sourceMappingURL=htmlAsRichText.cjs.map
