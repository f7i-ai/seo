import type { CustomTypeModelRichTextField, RichTextField, TitleField } from "@prismicio/client";
/**
 * Filter a rich text field based on given model.
 *
 * @param richTextField - A rich text or title field from Prismic.
 * @param model - A rich text or title field custom type model.
 *
 * @returns A rich text or title field filtered based on the given model.
 */
export declare const filterRichTextField: <Field extends RichTextField | TitleField>(richTextField: Field, model: CustomTypeModelRichTextField) => Field;
