import rehypeParse from "rehype-parse";
import { unified } from "unified";
import { rehypeRichText } from "./lib/rehypeRichText.js";
const htmlAsRichText = (html, config) => {
  const { result, messages } = unified().use(rehypeParse, { emitParseErrors: true, missingDoctype: 0 }).use(rehypeRichText, config).processSync(html);
  return { result, warnings: messages.map((message) => message.toString()) };
};
export {
  htmlAsRichText
};
//# sourceMappingURL=htmlAsRichText.js.map
